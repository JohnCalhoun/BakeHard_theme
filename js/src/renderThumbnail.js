goog.provide('bakehard.renderThumbnail');

goog.require('bakehard.templates');

bakehard.renderThumbnail.render=function(){
    //json to html
}
bakehard.renderThumbnail.append=function(){
    //take list of json
    //render each json
    //append to grid
}
bakehard.renderThumbnail.load=function(){
    //calculate page
    //load in new json
    //call append
}

