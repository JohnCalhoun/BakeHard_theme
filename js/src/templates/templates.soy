{namespace bakehard.templates}
{template .thumbnail}
{if $img_url }
    <div class="card medium stick-action { $category_string}">
        <div class="card-image">
            <img class='activator' src='{ $img_url}'>
        </div>
{else}
    <div class="card small stick-action { $category_string}">
{/if}
    <div class="card-content">
        <span class="card-title activate">{ $title.rendered}</span>
    </div>
    <div    class="card-action">
        <a  href="{ $link}" class='post-link'>view</a>
    </div>
    <div class="card-reveal">
        { $excerpt.rendered}
    </div>
</div>
{/template}

{template .loading}
    <div class="content loading-screen" id="loading-screen">
        loading
    </div>
{/template}

{template .loadingThumbnail}
    <div class="loading-thumbnail" id="loading-thumbnail">
        loading
    </div>
{/template}



